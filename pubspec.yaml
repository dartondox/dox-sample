name: dox_sample
publish_to: 'none'

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  dox_core: ^0.0.5
  postgres: ^2.6.1
  dox_query_builder: ^1.0.3
  json_annotation: ^4.8.0
  path: ^1.8.3
  watcher: ^1.0.2

dev_dependencies:
  json_serializable: ^6.6.1
  build_runner: ^2.3.3
  lints: ^2.0.0
  test: ^1.21.0
 
assets:
  - .env