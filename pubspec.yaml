name: dox_sample
publish_to: 'none'

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  dox_core: ^1.0.2
  postgres_pool: ^2.1.6
  dox_query_builder: ^1.1.11

dev_dependencies:
  dox_builder: ^1.0.0
  build_runner: ^2.3.3
  lints: ^2.0.0
  test: ^1.21.0
  http: ^1.0.0

assets:
  - .env